node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Env — .env.production is committed (URLs are public domains, not
# secrets) so EAS Build and `eas update` always inline the right base
# URLs regardless of who's running the build.
.env*
!.env.example
!.env.production
